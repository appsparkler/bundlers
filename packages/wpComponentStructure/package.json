{
  "name": "@me/wpComponentStructure",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "serve": "serve dist",
    "build-prod": "cross-env NODE_ENV=production webpack --config ./.webpack/index.js",
    "build-dev": "cross-env NODE_ENV=development webpack --config ./.webpack/index.js",
    "clean:build-dev": "yarn clean && yarn build-dev",
    "clean:build-prod": "yarn clean && yarn build-prod",
    "clean:build-dev:serve": "yarn clean && yarn build-dev && yarn serve",
    "clean:build-prod:serve": "yarn clean && yarn build-prod && yarn serve",

    "start": "cross-env NODE_ENV=development webpack-dev-server --open --config ./.webpack"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.5.5",
    "@babel/preset-react": "^7.0.0",
    "babel-loader": "^8.0.6",
    "dotenv": "^8.1.0",
    "eslint": "^6.3.0",
    "html-webpack-plugin": "^3.2.0",
    "rimraf": "^3.0.0",
    "serve": "^11.1.0",
    "webpack": "^4.39.3",
    "webpack-cli": "^3.3.7",
    "webpack-dev-server": "^3.8.0"
  },
  "dependencies": {
    "cross-env": "^5.2.1",
    "jquery": "^3.4.1",
    "prop-types": "^15.7.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0"
  }
}
